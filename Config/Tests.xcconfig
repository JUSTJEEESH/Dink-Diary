#include "Shared.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = $(DD_BUNDLE_ID_BASE).tests
SDKROOT = iphoneos
IPHONEOS_DEPLOYMENT_TARGET = 17.0
TARGETED_DEVICE_FAMILY = 1
SUPPORTED_PLATFORMS = iphoneos iphonesimulator

TEST_HOST = $(BUILT_PRODUCTS_DIR)/DinkDiary.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/DinkDiary
BUNDLE_LOADER = $(TEST_HOST)
