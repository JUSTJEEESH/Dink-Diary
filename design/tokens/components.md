# Dink Diary — Component Specs

> **Status: awaiting export from Claude Design.**
> Paste the locked component specs from your Claude Design project here, then delete this note.

Expected contents (per DESIGN-BRIEF.md):

- Session card, game row, stat tile, partner avatar cluster, streak badge, share card frame (9:16 and 1:1), pill button, tab bar
- Each in default, pressed, and empty states
- The signature court-line inset motif: the chosen interpretation and its exact spec (inset, stroke, radius)
- Design intent notes: spacing logic, motion specs (≤400ms), state behavior
